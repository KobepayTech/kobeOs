// Moved to the shared site-builder framework so every module's editor uses the
// same responsive preview. Re-exported here to keep existing imports working.
export { DevicePreviewFrame } from '@/components/site-builder/DevicePreviewFrame';
