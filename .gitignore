node_modules
dist
dist2/
.DS_Store
release/
*.AppImage
*.deb
*.exe
*.dmg
*.iso
iso-build/
live-build/
electron/server-bundle/
electron/.staging/
build/vcredist/vc_redist.x64.exe
build/cloudflared/
tmp-screenshots/

# Secrets — keep .env.example tracked, never commit real env files.
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.keystore
# HuggingFace cache (tokens live here after `huggingface-cli login`)
.huggingface/
**/.cache/huggingface/

# Local handwriting-OCR model cache (1.3 GB — pulled by prefetch script,
# not part of the source tree). Keep the path explicit so a stray
# `git add .` can't accidentally commit it.
server/node_modules/@huggingface/transformers/.cache/

