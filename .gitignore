node_modules
dist
dist2/
.DS_Store
release/
*.AppImage
*.deb
*.exe
*.dmg
*.iso
iso-build/
live-build/
electron/server-bundle/
electron/.staging/
build/vcredist/vc_redist.x64.exe
build/cloudflared/
tmp-screenshots/

# Secrets — keep .env.example tracked, never commit real env files.
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.keystore
# HuggingFace cache (tokens live here after `huggingface-cli login`)
.huggingface/
**/.cache/huggingface/

# transformers.js on-disk model cache — populated by translation /
# image-edit services that pull ONNX weights at runtime.
server/node_modules/@huggingface/transformers/.cache/

