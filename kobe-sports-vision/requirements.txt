# KobeSports vision worker.
# NOTE: ultralytics is AGPL-3.0 / Enterprise — see README before commercial use.
ultralytics>=8.3.0
supervision>=0.26.0
# trackers  # future-proof ByteTrackTracker (Supervision >=0.31 removes sv.ByteTrack)
opencv-python>=4.9.0
numpy>=1.26.0
PyYAML>=6.0        # optional (JSON configs work without it)
